Rapid7 github - https://github.com/rapid7/metasploit-framework/tree/master/modules/exploits
	if an exploit did not show up in the MSF search, we can copy the code from this repo onto the directory that our local install of MSF is referencing
	to find the exploit directory, type in mfs console: `locate exloits`

